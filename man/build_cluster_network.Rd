% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{build_cluster_network}
\alias{build_cluster_network}
\title{Build cluster network}
\usage{
build_cluster_network(
  dt,
  cluster_col = "chrom_cluster_id",
  read_col = "name",
  chrom_col = "chrom",
  start_col = "start",
  end_col = "end",
  id_col = "cluster_id",
  include_noise = FALSE,
  restrict_within_chrom = FALSE,
  min_shared = 1L,
  max_iter = 5L,
  verbose = TRUE
)
}
\arguments{
\item{dt}{A \code{data.table} from \code{cluster_reads_sweep()},
\code{cluster_reads_dbscan()}, or \code{cluster_reads_kde()}.}

\item{cluster_col}{Name of the cluster label column. Default
\code{"chrom_cluster_id"}.}

\item{read_col}{Name of the read name column. Default \code{"name"}.}

\item{chrom_col}{Name of the chromosome column. Default \code{"chrom"}.}

\item{start_col}{Name of the start coordinate column. Default \code{"start"}.}

\item{end_col}{Name of the end coordinate column. Default \code{"end"}.}

\item{id_col}{Name of the numeric cluster ID column (used for noise
filtering). Default \code{"cluster_id"}.}

\item{include_noise}{Logical. If \code{FALSE} (default), rows with
\code{cluster_id == 0} are excluded.}

\item{restrict_within_chrom}{Logical. If \code{TRUE}, only edges between
clusters on the same chromosome are kept.}

\item{min_shared}{Integer. Minimum number of shared reads for an edge.
Default \code{1L}.}

\item{max_iter}{Integer. Maximum number of filtering iterations. Default
\code{5L}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A named list with three elements:
  \describe{
    \item{nodes}{A \code{data.table} with columns \code{cluster},
      \code{chrom}, \code{cluster_n}, \code{node_avg_score},
      \code{node_avg_len}, \code{min_start}, \code{max_end},
      \code{edge_count}, \code{total_length}.}
    \item{edges}{A \code{data.table} with columns \code{cluster1},
      \code{cluster2}, \code{shared_reads}, \code{shared_avg_score},
      \code{shared_avg_len}.}
    \item{reads}{Rows from \code{dt} for reads that appear in exactly two
      of the kept clusters.}
  }
}
\description{
Takes a clustered read table and builds a network where nodes
  are clusters and edges connect clusters that share at least
  \code{min_shared} read names. An iterative filter ensures that the
  returned reads appear in exactly two of the kept clusters.
}
\details{
Build a shared-read cluster network
}
\examples{
\dontrun{
clusters <- cluster_reads_sweep(split_reads, delta = 1000)
net <- build_cluster_network(clusters, min_shared = 2)
}

}
\seealso{
Other network: 
\code{\link{shared_reads_between}()}
}
\concept{network}
