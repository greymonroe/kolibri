% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{nodes_to_fasta}
\alias{nodes_to_fasta}
\title{Export node sequences to FASTA}
\usage{
nodes_to_fasta(
  net,
  genome,
  clusters = NULL,
  chrom_col = "chrom",
  start_col = "min_start",
  end_col = "max_end",
  name_col = "cluster",
  pad_bp = 0L,
  index0 = TRUE,
  clamp = TRUE,
  tolower = FALSE,
  outfile = NULL,
  width = 80
)
}
\arguments{
\item{net}{A network list from \code{\link{build_cluster_network}}.}

\item{genome}{A named list of character vectors or \code{seqinr}-style
FASTA sequences (e.g. loaded with \code{seqinr::read.fasta()}).}

\item{clusters}{Optional character vector of cluster names to export. If
\code{NULL} (default), all nodes are exported.}

\item{chrom_col}{Name of the chromosome column in \code{net$nodes}. Default
\code{"chrom"}.}

\item{start_col}{Name of the start column in \code{net$nodes}. Default
\code{"min_start"}.}

\item{end_col}{Name of the end column in \code{net$nodes}. Default
\code{"max_end"}.}

\item{name_col}{Name of the node name column. Default \code{"cluster"}.}

\item{pad_bp}{Integer. Padding in bp to add on each side of the interval.
Default \code{0L}.}

\item{index0}{Logical. If \code{TRUE} (default), coordinates are assumed to
be 0-based (BED convention) and converted to 1-based R indexing.}

\item{clamp}{Logical. Clamp coordinates to chromosome bounds. Default
\code{TRUE}.}

\item{tolower}{Logical. Convert sequence to lowercase. Default \code{FALSE}.}

\item{outfile}{Optional path to write a FASTA file. Default \code{NULL}.}

\item{width}{Integer. FASTA line width when writing. Default \code{80}.}
}
\value{
A named character vector of sequences (one element per node).
}
\description{
Extracts the genomic sequences corresponding to network node
  spans from a loaded FASTA genome and returns them as a named character
  vector. Optionally writes a FASTA file.
}
\details{
Export network node sequences to FASTA
}
\examples{
\dontrun{
genome <- seqinr::read.fasta(
  system.file("extdata", "Col-CEN_v1.2.fasta.gz", package = "kolibri"))
seqs <- nodes_to_fasta(net, genome, outfile = "nodes.fa")
}

}
\concept{export}
