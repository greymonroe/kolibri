% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{shared_reads_between}
\alias{shared_reads_between}
\title{Shared reads between two clusters}
\usage{
shared_reads_between(
  dt,
  a,
  b,
  id_type = c("chrom_cluster_id", "cluster_id"),
  chrom_a = NULL,
  chrom_b = NULL,
  read_col = "name",
  chrom_col = "chrom",
  return = c("reads", "rows")
)
}
\arguments{
\item{dt}{A \code{data.table} produced by one of the
\code{cluster_reads_*()} functions.}

\item{a}{Identifier for the first cluster (character if
\code{id_type = "chrom_cluster_id"}, integer if
\code{id_type = "cluster_id"}).}

\item{b}{Identifier for the second cluster.}

\item{id_type}{Which cluster ID column to use: \code{"chrom_cluster_id"}
(default) or \code{"cluster_id"}.}

\item{chrom_a}{Optional chromosome for cluster \code{a} (needed when
\code{id_type = "cluster_id"} and IDs are not globally unique).}

\item{chrom_b}{Optional chromosome for cluster \code{b}.}

\item{read_col}{Name of the read name column. Default \code{"name"}.}

\item{chrom_col}{Name of the chromosome column. Default \code{"chrom"}.}

\item{return}{Either \code{"reads"} (character vector of shared read names)
or \code{"rows"} (rows from \code{dt} for those reads, tagged with
\code{cluster_side}).}
}
\value{
A character vector (if \code{return = "reads"}) or a
  \code{data.table} (if \code{return = "rows"}).
}
\description{
Returns the read names (or rows) that appear in both cluster
  \code{a} and cluster \code{b}.
}
\details{
Find reads shared between two clusters
}
\examples{
\dontrun{
shared <- shared_reads_between(clusters, a = "Chr1_12000", b = "Chr3_45000")
}

}
\seealso{
Other network: 
\code{\link{build_cluster_network}()}
}
\concept{network}
