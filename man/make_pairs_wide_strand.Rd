% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy.R
\name{make_pairs_wide_strand}
\alias{make_pairs_wide_strand}
\title{Paired reads in wide format}
\usage{
make_pairs_wide_strand(
  bed,
  score_min = 60,
  length_min = 1000,
  chroms = 1:5,
  chrom_prefix = "Chr",
  require_exact_pairs = TRUE
)
}
\arguments{
\item{bed}{A \code{data.table} from \code{\link{read_bed12}}.}

\item{score_min}{Minimum mapping score. Default \code{60}.}

\item{length_min}{Minimum aligned length. Default \code{1000}.}

\item{chroms}{Integer vector of chromosome numbers. Default \code{1:5}.}

\item{chrom_prefix}{Character prefix for chromosome names. Default
\code{"Chr"}.}

\item{require_exact_pairs}{Logical. If \code{TRUE} (default), keep only
reads with exactly 2 segments.}
}
\value{
A wide-format \code{data.table} with columns \code{name},
  \code{chrom_1}, \code{chrom_2}, \code{start_1}, \code{start_2},
  \code{end_1}, \code{end_2}, \code{strand_1}, \code{strand_2}.
}
\description{
Filters a BED table to reads with exactly two alignment
  segments on specified chromosomes and reshapes to one row per read pair
  with columns for each segment's chromosome, start, end, and strand.
}
\details{
Reshape paired reads to wide format (legacy)
}
\seealso{
Other legacy: 
\code{\link{cluster_pairs_bigwindows}()},
\code{\link{plot_primary_secondary_bins}()},
\code{\link{prep_primary_secondary_bins}()},
\code{\link{reads_near_region}()}
}
\concept{legacy}
