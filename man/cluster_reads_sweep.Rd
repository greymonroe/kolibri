% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_reads_sweep}
\alias{cluster_reads_sweep}
\title{Sweep-based read clustering}
\usage{
cluster_reads_sweep(
  dt,
  chrom_col = "chrom",
  start = "start",
  end = "end",
  delta = 0L,
  max_gap = Inf,
  max_span = NULL,
  warn_if_clusters_closer_than = NA_real_,
  id_scope = c("global"),
  verbose = TRUE
)
}
\arguments{
\item{dt}{A \code{data.table} with at least \code{chrom_col}, \code{start},
and \code{end} columns.}

\item{chrom_col}{Name of the chromosome column. Default \code{"chrom"}.}

\item{start}{Name of the start coordinate column. Default \code{"start"}.}

\item{end}{Name of the end coordinate column. Default \code{"end"}.}

\item{delta}{Integer. Maximum gap (bp) for merging consecutive intervals
during the sweep. Default \code{0L} (overlapping only).}

\item{max_gap}{Numeric. After sweeping, split any cluster whose consecutive
sorted reads are separated by more than this many bp. Default \code{Inf}
(no splitting).}

\item{max_span}{Deprecated alias for \code{max_gap}.}

\item{warn_if_clusters_closer_than}{Numeric. Warn when two cluster centres
are closer than this many bp. Default \code{NA} (no warnings).}

\item{id_scope}{Character. Either \code{"global"} (cluster IDs unique across
all chromosomes) or \code{"per_chrom"} (IDs restart per chromosome).}

\item{verbose}{Logical. Print split/warning messages. Default \code{TRUE}.}
}
\value{
The input \code{data.table} with three new columns: \code{cluster_id}
  (integer), \code{cluster_n} (number of reads in the cluster), and
  \code{chrom_cluster_id} (character label, e.g. \code{"Chr1_12345678"}).
}
\description{
Clusters genomic intervals per chromosome by a greedy
  left-to-right sweep: two consecutive intervals are merged when the gap
  between them is at most \code{delta} bp. After sweeping, any cluster
  with an internal gap larger than \code{max_gap} is split.
}
\details{
Cluster reads using a sweep algorithm
}
\examples{
\dontrun{
bed <- read_bed12(system.file("extdata", "alignVA2.bed12", package = "kolibri"))
split_reads <- bed[nameN > 1 & score > 30]
clusters <- cluster_reads_sweep(split_reads, delta = 1000, max_gap = 2000)
}

}
\seealso{
Other clustering: 
\code{\link{cluster_reads_dbscan}()},
\code{\link{cluster_reads_kde}()}
}
\concept{clustering}
