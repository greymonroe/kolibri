% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_reads_kde}
\alias{cluster_reads_kde}
\title{KDE-based read clustering}
\usage{
cluster_reads_kde(
  dt,
  chrom_col = "chrom",
  start = "start",
  end = "end",
  bw = NULL,
  bw_method = c("SJ", "nrd0", "ucv", "bcv"),
  bw_adjust = 1,
  kernel = "gaussian",
  max_gap = Inf,
  max_span = NULL,
  warn_if_clusters_closer_than = NA_real_,
  id_scope = c("per_chrom", "global"),
  verbose = TRUE,
  unassigned_label = 0L
)
}
\arguments{
\item{dt}{A \code{data.table} with at least \code{chrom_col}, \code{start},
and \code{end} columns.}

\item{chrom_col}{Name of the chromosome column. Default \code{"chrom"}.}

\item{start}{Name of the start coordinate column. Default \code{"start"}.}

\item{end}{Name of the end coordinate column. Default \code{"end"}.}

\item{bw}{Numeric bandwidth. If \code{NULL}, computed automatically via
\code{bw_method}.}

\item{bw_method}{Bandwidth selection method: \code{"SJ"} (default),
\code{"nrd0"}, \code{"ucv"}, or \code{"bcv"}.}

\item{bw_adjust}{Multiplicative adjustment to the computed bandwidth.
Default \code{1}.}

\item{kernel}{Kernel name passed to \code{\link[stats]{density}}. Default
\code{"gaussian"}.}

\item{max_gap}{Post-clustering gap split threshold. Default \code{Inf}.}

\item{max_span}{Deprecated alias for \code{max_gap}.}

\item{warn_if_clusters_closer_than}{Numeric warning threshold. Default
\code{NA}.}

\item{id_scope}{\code{"per_chrom"} or \code{"global"}.}

\item{verbose}{Logical. Default \code{TRUE}.}

\item{unassigned_label}{Integer label for unassigned reads. Default
\code{0L}.}
}
\value{
The input \code{data.table} with \code{cluster_id}, \code{cluster_n},
  and \code{chrom_cluster_id} columns added.
}
\description{
Clusters genomic intervals per chromosome by fitting a weighted
  kernel density estimate to the coverage landscape and assigning each
  interval to the nearest density mode. Intervals further than
  \code{max_gap} from any mode are labelled as unassigned.
}
\details{
Cluster reads using kernel density estimation
}
\examples{
\dontrun{
clusters <- cluster_reads_kde(split_reads, bw_method = "SJ", bw_adjust = 1.5)
}

}
\seealso{
Other clustering: 
\code{\link{cluster_reads_dbscan}()},
\code{\link{cluster_reads_sweep}()}
}
\concept{clustering}
