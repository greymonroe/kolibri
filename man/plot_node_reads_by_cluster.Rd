% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network.R
\name{plot_node_reads_by_cluster}
\alias{plot_node_reads_by_cluster}
\title{Plot reads by cluster for a node}
\usage{
plot_node_reads_by_cluster(
  net,
  node,
  cluster_col = "chrom_cluster_id",
  read_id_col = "name",
  start_col = "start",
  end_col = "end",
  alpha_col = "score",
  alpha_range = c(0.3, 1),
  primary_color = "red2",
  partner_color = "grey60",
  base_size = 6,
  ensure_strip_space = TRUE,
  min_facet_width_mm = 18,
  strip_wrap_width = 20
)
}
\arguments{
\item{net}{A network list from \code{\link{build_cluster_network}}.}

\item{node}{Character. The \code{chrom_cluster_id} of the primary node.}

\item{cluster_col}{Name of the cluster column in \code{net$reads}. Default
\code{"chrom_cluster_id"}.}

\item{read_id_col}{Name of the read name column. Default \code{"name"}.}

\item{start_col}{Name of the start coordinate column. Default \code{"start"}.}

\item{end_col}{Name of the end coordinate column. Default \code{"end"}.}

\item{alpha_col}{Column to map to transparency (e.g. \code{"score"}). Default
\code{"score"}.}

\item{alpha_range}{Numeric vector of length 2. Default \code{c(0.3, 1.0)}.}

\item{primary_color}{Colour for segments in the primary node. Default
\code{"red2"}.}

\item{partner_color}{Colour for segments in partner nodes. Default
\code{"grey60"}.}

\item{base_size}{Base font size. Default \code{6}.}

\item{ensure_strip_space}{Logical. Apply theme tweaks for readable strip
labels. Default \code{TRUE}.}

\item{min_facet_width_mm}{Minimum facet width in mm (used when ggh4x is
available). Default \code{18}.}

\item{strip_wrap_width}{Character wrap width for strip labels. Default
\code{20}.}
}
\value{
A \code{ggplot2} plot object.
}
\description{
For a chosen primary node, plots all reads belonging to it
  across the primary cluster and any partner clusters it is connected to in
  the network. Reads are arranged with one consistent y-position per read
  name. Facets represent individual clusters.
}
\details{
Plot reads for a node and all its network partners
}
\examples{
\dontrun{
plot_node_reads_by_cluster(net, node = "Chr1_12953910")
}

}
\seealso{
Other visualization: 
\code{\link{genome_from_bed}()},
\code{\link{make_genome_map}()},
\code{\link{map_to_genome_x}()},
\code{\link{plot_bed_igv}()},
\code{\link{plot_cluster_network_ggraph}()},
\code{\link{plot_genome_arrows_flat}()},
\code{\link{plot_genome_arrows_linear}()},
\code{\link{plot_reads_around_node}()}
}
\concept{visualization}
