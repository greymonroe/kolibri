% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_network.R
\name{plot_cluster_network_ggraph}
\alias{plot_cluster_network_ggraph}
\title{Plot shared-read cluster network}
\usage{
plot_cluster_network_ggraph(
  net,
  layout = "fr",
  cap = 30,
  alpha_var = "node_avg_score",
  alpha_range = c(0.3, 1),
  label = FALSE,
  label_col = "cluster",
  label_size = 2,
  label_repel = TRUE,
  seed = 1,
  min_cluster_n = 1L,
  keep_neighbors_of_kept = TRUE
)
}
\arguments{
\item{net}{A network list from \code{\link{build_cluster_network}}.}

\item{layout}{ggraph layout algorithm. Default \code{"fr"}
(Fruchterman-Reingold). Other options: \code{"kk"}, \code{"drl"}, etc.}

\item{cap}{Maximum node size (cluster_n capped at this value). Default
\code{30}.}

\item{alpha_var}{Column in \code{net$nodes} to map to node transparency.
Default \code{"node_avg_score"}. Set to \code{NULL} for uniform alpha.}

\item{alpha_range}{Numeric vector of length 2 giving the alpha range.
Default \code{c(0.3, 1.0)}.}

\item{label}{Logical. Whether to label nodes. Default \code{FALSE}.}

\item{label_col}{Column to use for node labels. Default \code{"cluster"}.}

\item{label_size}{Text size for labels. Default \code{2}.}

\item{label_repel}{Logical. Use repelling labels. Default \code{TRUE}.}

\item{seed}{Random seed for layout reproducibility. Default \code{1}.}

\item{min_cluster_n}{Minimum \code{cluster_n} (or \code{edge_count}) for a
node to be retained. Default \code{1L} (keep all).}

\item{keep_neighbors_of_kept}{Logical. Retain direct neighbours of kept
nodes even if they fall below \code{min_cluster_n}. Default \code{TRUE}.}
}
\value{
A \code{ggraph}/\code{ggplot2} plot object.
}
\description{
Renders a force-directed (or other layout) graph of the cluster
  network. Nodes are scaled by cluster size, coloured by chromosome, and
  optionally labelled. Low-count nodes can be filtered while keeping their
  direct neighbours.
}
\details{
Plot cluster network with ggraph
}
\examples{
\dontrun{
net <- build_cluster_network(clusters)
plot_cluster_network_ggraph(net, label = TRUE, min_cluster_n = 3)
}

}
\seealso{
Other visualization: 
\code{\link{genome_from_bed}()},
\code{\link{make_genome_map}()},
\code{\link{map_to_genome_x}()},
\code{\link{plot_bed_igv}()},
\code{\link{plot_genome_arrows_flat}()},
\code{\link{plot_genome_arrows_linear}()},
\code{\link{plot_node_reads_by_cluster}()},
\code{\link{plot_reads_around_node}()}
}
\concept{visualization}
