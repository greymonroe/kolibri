% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_reads_dbscan}
\alias{cluster_reads_dbscan}
\title{DBSCAN-based read clustering}
\usage{
cluster_reads_dbscan(
  dt,
  chrom_col = "chrom",
  start = "start",
  end = "end",
  distance = c("gap", "euclidean", "linf"),
  eps = NULL,
  minPts = 3L,
  max_gap = Inf,
  max_span = NULL,
  warn_if_clusters_closer_than = NA_real_,
  id_scope = c("per_chrom", "global"),
  verbose = TRUE
)
}
\arguments{
\item{dt}{A \code{data.table} with at least \code{chrom_col}, \code{start},
and \code{end} columns.}

\item{chrom_col}{Name of the chromosome column. Default \code{"chrom"}.}

\item{start}{Name of the start coordinate column. Default \code{"start"}.}

\item{end}{Name of the end coordinate column. Default \code{"end"}.}

\item{distance}{Distance metric: \code{"gap"}, \code{"euclidean"}, or
\code{"linf"}.}

\item{eps}{DBSCAN epsilon radius. If \code{NULL}, defaults to the maximum
interval length on the chromosome.}

\item{minPts}{DBSCAN minimum points. Default \code{3L}.}

\item{max_gap}{Post-clustering gap split threshold. Default \code{Inf}.}

\item{max_span}{Deprecated alias for \code{max_gap}.}

\item{warn_if_clusters_closer_than}{Numeric. Warn when cluster centres are
closer than this many bp. Default \code{NA}.}

\item{id_scope}{\code{"per_chrom"} or \code{"global"}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The input \code{data.table} with \code{cluster_id}, \code{cluster_n},
  and \code{chrom_cluster_id} columns added.
}
\description{
Clusters genomic intervals per chromosome using the DBSCAN
  density-based algorithm. Three distance metrics are supported: gap-based
  (distance = max(0, gap between intervals)), Euclidean (on midpoint and
  length), and L-infinity.
}
\details{
Cluster reads using DBSCAN
}
\examples{
\dontrun{
clusters <- cluster_reads_dbscan(split_reads, eps = 5000, minPts = 3)
}

}
\seealso{
Other clustering: 
\code{\link{cluster_reads_kde}()},
\code{\link{cluster_reads_sweep}()}
}
\concept{clustering}
