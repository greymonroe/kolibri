% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_io.R
\name{read_bed12}
\alias{read_bed12}
\title{Read a BED12 alignment file}
\usage{
read_bed12(path, key_on = c("chrom", "start", "end"))
}
\arguments{
\item{path}{Character. Path to the BED12 file.}

\item{key_on}{Character vector of column names to use as the data.table key.
Default is \code{c("chrom","start","end")}. Set to \code{NULL} to skip
keying.}
}
\value{
A keyed \code{data.table} with the 12 standard BED columns plus
  \code{is_split} (logical), \code{aligned_length} (integer), and
  \code{nameN} (integer count of occurrences of each read name).
}
\description{
Reads a BED12 file into a \code{data.table}. Multi-block rows
  (split reads with \code{blockCount > 1}) are accepted with a message.
  Derived columns \code{is_split}, \code{aligned_length}, and \code{nameN}
  are added automatically.
}
\details{
Read a BED12 file
}
\examples{
\dontrun{
bed <- read_bed12(system.file("extdata", "alignVA2.bed12", package = "kolibri"))
head(bed)
}

}
\concept{data I/O}
