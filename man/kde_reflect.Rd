% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kde.R
\name{kde_reflect}
\alias{kde_reflect}
\title{Reflection-corrected KDE}
\usage{
kde_reflect(
  x,
  bw = NULL,
  bw_method = c("SJ", "nrd0", "ucv", "bcv"),
  kernel = "gaussian",
  grid_n = 2048,
  reflect_multiplier = 3
)
}
\arguments{
\item{x}{Numeric vector of observations.}

\item{bw}{Numeric bandwidth. If \code{NULL}, computed from \code{bw_method}.}

\item{bw_method}{Bandwidth selection method: \code{"SJ"} (default),
\code{"nrd0"}, \code{"ucv"}, or \code{"bcv"}.}

\item{kernel}{Kernel name. Default \code{"gaussian"}.}

\item{grid_n}{Number of evaluation points. Default \code{2048}.}

\item{reflect_multiplier}{Points within this many bandwidths of a boundary
are reflected. Default \code{3}.}
}
\value{
A named list with elements \code{bw} (bandwidth used), \code{density}
  (the full \code{\link[stats]{density}} object), \code{modes} (x-positions
  of local maxima), and \code{mode_idx} (indices into the density grid).
}
\description{
Computes a kernel density estimate with boundary reflection to
  reduce edge bias. Points near the left and right boundaries are mirrored
  before estimation, and the density is evaluated only over the original
  data range.
}
\details{
Boundary-reflected kernel density estimate
}
\examples{
\dontrun{
res <- kde_reflect(c(1000, 2000, 2100, 3000, 5000))
plot(res$density)
}

}
\seealso{
Other KDE: 
\code{\link{estimate_modes_1d}()},
\code{\link{plot_node_breaks_kde}()}
}
\concept{KDE}
